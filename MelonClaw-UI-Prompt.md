请在当前 MelonClaw 仓库中直接优化现有 Web 前端，让它形成与“西瓜 + 瓜子爪印”相关的品牌视觉，同时改善聊天阅读与操作体验。请完成实际代码修改、必要验证和项目文档更新。

## 1. 先检查当前实现

阅读根目录 AGENTS.md，以及以下文件：
- src/melonclaw/web/static/index.html
- src/melonclaw/web/static/styles.css
- src/melonclaw/web/static/app.js
- src/melonclaw/web/app.py：仅用于核对已有接口契约
- README.md、note.md 中与 Web、Project、会话和审批有关的部分
- 当前静态图片，以及我提供的 MelonClaw-UI-Kit 资源包（如果已解压在工作区）

本任务参考的仓库版本是 dee268d5da1866750f1f0f302fcd89bd57a4ecb4。若当前代码已变化，按当前代码重新确认问题，不覆盖已有修改。项目当前使用 FastAPI 提供原生 HTML/CSS/JavaScript；在这一结构上迭代，无需迁移 React、Ant Design、Tailwind 或引入新构建框架。

优先修改现有静态文件，可按职责提取少量前端模块。保持 API 路径、请求与 SSE 事件结构、数据库和 Agent 配置不变。后端能力缺失时，不添加假按钮或伪造完成状态。

## 2. 品牌方向

产品主名称统一为 MelonClaw，中文辅助名称为“瓜爪助手”。目标是清爽、温暖、轻微可爱、适合长时间阅读的智能助手。

沿用现有 Logo 的三个棕色瓜子爪趾、红色西瓜掌垫、浅绿瓜皮和深绿外皮。品牌图形主要出现在侧栏、助手头像、欢迎页和 favicon；普通功能图标保持清晰的功能含义。

配色以奶白、白色和浅绿为大面积背景，瓜皮绿用于主要操作、选中态和链接，果肉红只做少量品牌点缀。近似比例为 85% 中性色、12% 绿色、3% 珊瑚红。避免大面积红绿渐变、通屏纹理、厚重阴影和持续动效。

用 CSS 变量统一管理，并替换旧样式中的零散硬编码颜色：
- --canvas: #F8FAF6
- --panel: #FFFFFF
- --sidebar: #F2F6F0
- --ink: #20392E
- --muted: #607266
- --line: #DCE5DC
- --brand-primary: #176B4A
- --brand-primary-hover: #12553B
- --brand-soft: #E8F4EA
- --brand-coral: #F56B70
- --brand-coral-soft: #FFF0ED
- --brand-seed: #403329

成功、等待审批、错误另设语义颜色；不能因为品牌含红色，就把错误状态也当装饰处理。珊瑚红上不要放小号白字；主按钮用深绿底白字。

字体采用系统字体栈，补充 PingFang SC、Microsoft YaHei。聊天正文 15–16px、行高 1.75；侧栏项目和会话标题 13–14px；辅助信息通常不低于 12px。欢迎标题桌面约 30–34px、手机约 26–28px。使用 4/8px 间距体系，控件圆角 10–12px、输入框约 18px，避免所有组件使用不同的特殊切角。

## 3. 品牌资源接入

如果资源包存在：
- 将 assets/melonclaw-mark-white.png 接入 /static/assets/brand/。它是白底 RGB 位图，不是透明 PNG，不得把白底当作透明背景。
- 侧栏图标约 36–40px，助手头像约 32px，欢迎页图标约 64–72px，使用 object-fit: contain。
- 图标与文字分离：侧栏呈现“纯图形 + MelonClaw 文本”，不要在 34px 容器里缩放整张带字海报。
- 图标源图为 1254×1254；使用现有可用的图片工具导出合适尺寸的 PNG/WebP，原始文件保留。浏览器图标生成适合 32/48px 的版本，并检查小尺寸可辨识性。首屏实际使用的品牌文件争取小于 80KB。
- 将 assets/icons/ 中用到的 Lucide SVG 按需接入项目，保留 LICENSE.txt 和来源说明。通过本地资源加载；不依赖运行时外部 CDN。

如果资源包不存在，继续完成布局和样式优化，临时沿用仓库已有 melon-claw.png，并在交付中说明待替换资源；不要自行画一个完全不同的新 Logo。

替换当前散落的 ◌、⌘、⌁、✦ 等装饰性功能符号，使用同一套 18–20px Lucide 图标。图标用 currentColor 或引用本地 SVG 的 CSS mask 着色；通过 img 引用外部 SVG 时，不要误以为它会继承父元素 currentColor。装饰图标 aria-hidden，只有图标的按钮必须有可访问名称。

用户头像应与助手品牌图标区分。优先使用真实用户头像；当前 API 无独立头像字段时用姓名首字与稳定的浅色背景，不要把同一张人物图作为所有用户的固定身份。保留原图片文件。

## 4. 布局与侧栏

保留桌面“左侧项目/会话导航 + 右侧聊天”的总体结构。桌面侧栏约 260px，支持收起；顶部 Logo，下面是“新建对话”主按钮，然后是项目列表与会话列表。底部放模拟用户选择和一行紧凑运行状态，模型与连接详情可展开。

项目区与会话区的归属要明确：会话列表上方显示“当前项目 · 项目名”或等价标题。目前会话容器位于整个项目列表后，不要仅靠左缩进把它画成最后一个项目的子列表。选中项目与选中会话均有清晰状态，项目名不要在每一条会话时间里重复。

保留会话独立滚动、固定行高和“加载更多”。字号增大后行高约 48–52px，主标题一行省略，时间为次行；长标题提供完整名称提示。侧栏底部区域不参与列表滚动。

模拟用户仍明确标注“模拟用户”，用户和租户数据继续来自 API，不能硬编码张三/李四名单。不要把模拟身份设计成已登录认证，也不要在没有相应交互和接口的情况下新增租户管理入口。

右侧顶部约 56–64px，高优先级信息是“当前项目 / 会话名称”。减少“瓜爪智能助手”在侧栏、顶栏和每条消息中的重复。聊天与输入框使用一致的内容轴线，正文最大宽度约 800px，两侧留白随视口缩放。

## 5. 首屏与空状态：必须改到运行时渲染逻辑

重点检查 loadConversations()、clearConversationView()、createConversation()、renderHistory()、updateComposer()：
当前 index.html 中的 #welcome 在初始化后会被 clearConversationView() 清除；只改静态欢迎 HTML 不会解决首屏问题。

建立可复用的欢迎/空状态渲染函数，或者使用 template 配合事件委托。欢迎页重新挂载后，快捷卡片的事件仍有效。

正常无消息状态显示：
- 无文字瓜爪图标
- “你好，我是 MelonClaw”
- 主标题：“今天，有什么想一起搞定的？”
- 辅助文案：“查资料、理思路、做计划，瓜爪来帮你。”
- 三个任务入口：“查找资料”“整理思路”“制定计划”

三个快捷入口用现有能力能执行的请求，例如比较两个技术方案、把文本整理成清单、制定一周学习计划。点击后仅填入输入框并聚焦，让用户可以修改后发送。

区分加载中、加载失败、无可用项目、项目无会话和空会话。正常首页不要长期只显示“选择一个项目”及禁用输入框。用户/项目加载完成、有可用项目且当前不忙时，允许用户先输入；首次发送如缺少会话，复用现有创建接口建立默认项目中的会话，再发送这条消息。无可用项目时给出“新建项目”入口；创建失败保留输入内容并提供明确反馈。

首次创建/发送要防连击和重复请求，沿用 request_id、generation、userId/tenantId/projectId 的校验，避免切换身份或项目后把迟到的创建结果及草稿发送到错误上下文。不要每次刷新首页都自动创建一个空会话。

## 6. 输入框与状态

输入框保持在主面板底部，桌面最大宽度与聊天正文对齐，左右间距一致。默认可见两行左右的输入空间，随内容增长并设置最大高度。移除缺乏信息量的“随时聊聊，我会帮你处理”等重复提示，保留简洁占位与 Enter / Shift+Enter 提示。

发送按钮有明确的禁用、可发送、发送中状态：空白内容不可发送；正在执行或等待审批时保持与当前后端行为一致。创建或发送失败时保留/恢复未成功提交的草稿；不要对结果不确定的请求自动重发。

中文输入法组合输入时 Enter 不得误发送，处理 isComposing/composition 状态。保留现有 Ctrl/Cmd+K 新建对话快捷键，提示按平台显示。

去掉永久静态 LIVE 标签。根据已有状态和 SSE 事件显示“连接中 / 已就绪 / 处理中 / 等待确认 / 失败”。模型名称来自接口；没有可靠联网状态时不要硬编码“网页查询已连接”。保持用户正在向上阅读时不被流式输出拉回底部。

当前没有确认的服务端取消接口，不添加会让用户误以为停止 Agent 执行的“停止”按钮。

## 7. 聊天阅读与工具过程

助手回答采用文档式排版；用户消息用低饱和浅绿气泡。消息角色清晰、留白克制。历史用户消息不得一律显示“刚刚”：有可靠时间字段则格式化显示，否则省略时间，不编造。

当前 renderHistory() 与 SSE 分支都使用 textContent，Markdown 标题、列表、链接、代码块和表格会以原始文本显示。增加轻量 Markdown 渲染，历史与实时完成结果使用相同管线：
- 维护原始消息字符串作为数据源，复制功能复制该原文。
- Markdown 解析后进行 HTML 清洗，再插入 DOM。可使用固定版本的本地 Marked + DOMPurify，或项目已有等价方案；Marked 本身不清洗 HTML。
- 用户消息、工具参数、工具原始输出继续以文本安全渲染。
- 代码块与宽表格在自身容器内横向滚动，不撑破页面。
- 默认不自动加载模型输出中的远程图片，避免无意请求外部资源；外链限制安全协议，新增窗口链接加适当 rel。
- 流式阶段可先安全显示纯文本，在完成时统一渲染 Markdown；或节流渲染。不能每个 token 重建整条消息与工具树。
- 为完整回复和代码块提供复制，并显示实际复制成功/失败状态。

保留现有工具 details/summary 和“完成后收起”的机制，它已经存在。进一步提高 summary 字号与对比度，显示“搜索资料 / 读取文件 / 执行计算”等可理解名称；原始工具名保留在详情或辅助位置。未知工具名原样回退，不猜测用途。

保留 call_key、subagent_id、父子映射、历史 events 恢复和失败详情。运行、完成、失败同时用图标与文字表达，不只换颜色。不要把执行日志、JSON 以及全部子 Agent 输出持续占据聊天主区域。

## 8. 人工审批界面

保留 approve/edit/reject/respond 与 allowed_decisions 约束，工具名称不可被 UI 改写。品牌优化不能改变审批语义。

审批标题改得更直接，例如“需要你确认一项操作”。优先显示实际动作、目标文件/命令、参数摘要；完整参数可展开。批准、编辑参数、拒绝等操作要清晰，特别是编辑 JSON 的校验错误应贴近对应表单项。

动作列表较长时限制审批区域高度并内部滚动，约不超过可用视口高度的 40%，确保决定提交区域可见，聊天上下文不被完全挤走。等待确认用琥珀色；正文与工具信息维持可读对比度。

## 9. 手机与键盘可用性

当前 max-width:620px 把侧栏放在聊天区上方，同时主面板仍占 100dvh，结构上会显著增加整页高度。改成：
- <=768px：顶部菜单按钮打开侧栏抽屉；默认收起，聊天占主视口。
- 抽屉有遮罩、关闭按钮、Esc 关闭、焦点约束和关闭后的焦点恢复；背景内容不抢焦点。
- 选择会话后关闭抽屉；手机快捷卡片一列排列。
- 输入区域考虑 100dvh 和 env(safe-area-inset-bottom)，横向无整页溢出。验证虚拟键盘弹出时输入框可见；若环境无法测试真实键盘，明确列为未验证。
- 点击区域尽量至少 44px，按钮/链接/表单具有清晰 focus-visible。
- 保留 aria-live 状态反馈，避免每个 token 都打断读屏；兼容 prefers-reduced-motion。
- 常规正文与背景对比度目标至少 4.5:1，不能用 9px 浅灰字承载必要信息。

新增项目可将 window.prompt 替换为与主题一致的 dialog，包含名称、取消和创建，支持键盘与错误反馈；仍调用原来的 POST /api/projects。

## 10. 实施顺序与验证

先修正常空状态与输入状态，再做统一 tokens、侧栏和品牌资源，然后完善 Markdown/工具/审批展示，最后处理响应式与键盘体验。不要顺手添加上传、语音、文件管理器、模型切换等缺少后端支持的新功能。

按 AGENTS.md 更新 note.md；用户可见行为发生变化时同步 README.md，写清变更缘由、实际使用方式与验证边界。

执行与本次修改匹配的检查：
- node --check src/melonclaw/web/static/app.js，以及新增 JS 文件
- git diff --check
- 新增本地依赖记录固定版本及许可证，检查实际引用均存在
- 正常启动现有应用，或在后端不可用时使用明确标注的临时 API fixture 验证前端。fixture 不进入生产路径、不连接真实模型和数据库

浏览器验收覆盖桌面 1440×900、紧凑屏幕 1024×768、手机 390×844，至少记录：
1. 用户/项目加载完成后的实际欢迎页，不是初始化前一闪而过的 HTML；
2. 首条消息创建并发送、重复点击、创建失败草稿保留；
3. 有长回答、Markdown 表格/链接/代码和工具调用的会话；
4. 子 Agent 事件、历史恢复、切换用户/项目时迟到响应不会串入；
5. 等待审批与多项审批，编辑 JSON、拒绝理由和失败重试；
6. 侧栏长列表、长会话名、移动抽屉、键盘操作及中文输入法；
7. 页面无横向溢出、聊天与导航分别滚动、滚动位置不被抢走。

Markdown 清洗和首条消息异步创建是本次需要重点验证的行为变化；使用能覆盖实际风险的检查，避免为纯颜色和间距写冗余测试。

交付时给出修改文件、主要变化、已完成验证、前后对照截图和仍未验证的项。若浏览器或真实后端不可用，准确报告限制，不声称已完成视觉/端到端验收。

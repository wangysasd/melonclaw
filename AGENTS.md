# DeepAgents 学习项目

这个项目用于学习 **DeepAgents** 框架。学习方式以实践为主：

- 用户会提供网页链接；先阅读链接内容，再用中文讲解其中的概念、设计取舍和关键代码。每次讲解都必须沉淀并整合进同一个持续演进的学习项目，禁止为单个链接创建独立项目、demo 或目录。
- 用户要求实践时，应扩展 `src/melonclaw/` 中的累计应用，并附上运行步骤、预期结果与可观察点。新的能力必须在既有代码和学习成果上迭代、扩展或重构，形成连贯的学习路径。
- 未指定模型时，运行 demo 默认使用 `.env` 中的 DeepSeek 配置（`DEEPSEEK_BASE_URL`、`DEEPSEEK_API_KEY`、`DEEPSEEK_MODEL`）。
- 用户明确指定模型或提供其他运行要求时，优先遵从用户的指定。
- 不要把 `.env` 中的 API 密钥输出到终端、日志、文档、示例代码或回复中；示例只读取环境变量。
- 新增依赖、修改项目结构或扩展应用后，给出简洁的运行命令与验证结果。

## 项目结构约定

- 应用代码统一维护在 `src/melonclaw/`，并按配置、Agent 构建、提示词、流式输出等职责拆分模块。不要创建 `demos/`、`examples/` 或 `tests/` 目录，也不要把所有代码集中在单一文件中。
- 每次新增知识点时，优先扩展既有模块或提炼可复用组件；只有职责足够独立时才在包内增加子包。目录和模块命名应反映领域职责，而非网页来源。
- 讲解内容和运行成果应持续更新到根目录 `quickstart_research.md`，并在根目录 README 中维护统一入口；Agent 运行中间文件使用系统临时目录，不写入仓库。
- 默认使用 Python；项目依赖以 `pyproject.toml` 和 `uv.lock` 为准，使用 `uv sync` 管理环境。`requirements.txt` 仅保留作 pip 兼容清单。

# MelonClaw 聊天附件上传技术设计

> 文档状态：设计中，尚未实现
>
> 文档用途：本文档既是附件功能的技术设计，也是后续开发 Agent 的实现 Prompt。后续讨论如产生新的约束、接口或取舍，应优先更新本文档的“决策记录”和对应章节。
>
> 适用范围：MelonClaw 当前 Project / Conversation / Agent / SSE 链路，支持聊天上传图片和文件，并为未来安全沙箱预留接入边界。
>
> 最近更新：2026-09-12，补入已对照 deepagents 0.7.5 / langchain-core 1.5.3 / langchain-openai 1.4.2 源码核实的框架事实（3.2、3.3、5、8、9、16），并新增待拍板问题（19.2）。未实测的 provider 行为一律标注为待验证，不作为结论。

## 0. 给开发 Agent 的执行要求

开发前必须先阅读仓库根目录 AGENTS.md、本文档和当前工作树状态。当前仓库可能存在用户未提交的前端、后端和 Skill 改动，不能用覆盖式重构或破坏性 Git 操作清理这些改动。

实现必须遵守以下要求：

1. 在现有 Web、数据库、Agent 组装和 SSE 链路上迭代，不创建独立的附件 Demo。
2. 所有用户、租户、Project、Conversation 和附件归属都必须由服务层重新校验，不能信任浏览器传入的 ID。
3. 不把文件原文、base64、API Key、Token、数据库密码或完整凭据 URL 写入日志、Prompt、SSE 或长期消息历史。
4. 当前 LocalShellBackend 不是安全沙箱。开发环境可以验证功能，但面向共享或不受信任用户的部署必须使用受控 SandboxBackend 或独立解析服务。
5. 新增 API、环境变量、依赖和用户可见行为时，同步更新 README.md；数据库变更同步更新迁移和 schema 版本。
6. 使用 apply_patch 修改代码；实现后运行与改动匹配的测试、类型检查、Lint、编译检查和 git diff --check。
7. 真实模型、OCR 服务、对象存储、PostgreSQL 和沙箱验证必须说明依赖、验证命令和观察点，不能假装本地单测已经覆盖外部服务。
8. 3.2 和 3.3 的框架事实已对照安装版本源码核实，实现时直接使用，不要重复实现能力声明系统或块转换器。
9. 3.2 中带「待验证」性质的 provider 行为（DeepSeek / MiniMax 是否接受具体内容块）以 Phase 0 实测为准，实测结果要回填到 9.2 的能力路由表和 19.2。

## 1. 背景与目标

当前聊天接口使用 JSON 请求和 SSE 响应。ExecutionService 在 Agent 执行前负责用户、Project、Conversation 归属校验、request_id 幂等、会话锁和消息落库；Agent 使用 Project 工作区和 Deep Agents 文件系统能力。

新增附件功能后，用户可以在聊天中上传图片、PDF、文本文件和常见办公文件，并在当前消息中引用它们。系统需要同时满足：

- 当前轮能让支持多模态的模型看到图片或 PDF；
- 不支持多模态的文本模型仍能通过 OCR 或文档解析使用附件文字；
- 大文件不被一次性塞进 Prompt、数据库或 Checkpointer；
- 附件能在历史消息、断线重连、HITL 恢复和 request_id 重试中被重新定位；
- 原始文件只读，Agent 生成或修改的文件进入 Project / outputs 区域；
- 未来切换到安全沙箱时，不需要重做数据库和聊天 API；
- 不因附件路径、文件名、MIME 或解析器引入越权、路径穿越和宿主机执行风险。

本功能不是把文件上传到一个公开静态目录，也不是把所有文件内容永久存储在聊天消息中。附件是独立的持久资源，消息只保存对附件的受控引用。

## 2. 非目标

第一阶段不实现以下内容：

- 让任意文件都能被模型理解；未知二进制只允许被拒绝，或只保留元数据，不承诺内容分析；
- 在主进程中无隔离地执行用户上传文件、Office 宏、脚本或压缩包；
- 自动将附件全部注入每一轮历史消息；
- 把 OCR 结果作为不可追溯的聊天正文写入 chat_messages.content；
- 依赖浏览器传入的 user_id、tenant_id、project_id 或附件路径实现授权；
- 在当前 LocalShellBackend 上宣称已经具备生产级多租户安全隔离。

## 3. 当前基线与框架能力

### 3.1 MelonClaw 当前基线

- 聊天入口：POST /api/conversations/{conversation_id}/messages，JSON 请求，SSE 响应。
- Project 工作区由 Settings.workspace_root 和数据库中的 Project.workdir_path 解析；默认配置使用 MELONCLAW_WORKSPACE_DIR，未配置时为用户目录下的 .melonclaw/workspaces。[config.py](/Users/yingshuowang/Documents/back/melonclaw/src/melonclaw/core/config.py:27)
- Agent 使用 CompositeBackend 和 Project 工作区；默认 LocalShellBackend 允许本地文件和 Shell 能力，但不是安全沙箱。[factory.py](/Users/yingshuowang/Documents/back/melonclaw/src/melonclaw/backend/factory.py:60)
- 当前模型目录只记录 provider、模型名和 OpenAI-compatible 适配类型，没有统一的 supports_vision、supports_pdf 等能力声明。[model_catalog.py](/Users/yingshuowang/Documents/back/melonclaw/src/melonclaw/core/model_catalog.py:27)
- 当前模型工厂统一通过 ChatOpenAI 适配 DeepSeek、MiniMax 和 OpenAI-compatible provider；最终是否接受图片或 PDF 由实际模型和供应商决定。[chat_model.py](/Users/yingshuowang/Documents/back/melonclaw/src/melonclaw/core/chat_model.py:10)

### 3.2 Deep Agents / LangChain 当前能力边界

当前依赖 deepagents==0.7.5、langchain-core 1.5.3、langchain-openai 1.4.2。本节结论均已对照安装版本源码核实，实现时不需要重新推导；源码路径以本机 `.venv` 为例。

**扩展名分类是一张硬编码表。** Deep Agents 的 FilesystemMiddleware 自带 read_file，按扩展名把文件分类后转换为多模态内容块，映射表在 `deepagents/backends/utils.py:30-60`：

- image：`.png/.jpeg/.jpg/.webp/.gif/.heic/.heif`
- video：`.mp4/.mpeg/.mov/.avi/.flv/.mpg/.webm/.wmv/.3gpp`
- audio：`.wav/.mp3/.aiff/.aac/.ogg/.flac`
- file：只有 `.pdf/.ppt/.pptx`

`.docx/.xlsx` 不在表中，会落到默认的 `text` 类型，并以 `encoding="utf-8"` 严格模式打开二进制文件（`deepagents/backends/filesystem.py:460-463`），结果是解码失败而不是文本提取。也就是说 read_file 对 Office 现代格式既不会解析，也不会伪装成文本。

read_file 对 PDF 的典型输出是：

~~~python
{
    "type": "file",
    "base64": "...",
    "mime_type": "application/pdf",
}
~~~

这一过程解决的是“文件如何从 Agent 文件系统进入模型消息”，不是通用 PDF OCR。它不会自动把扫描 PDF 转换成可搜索文本，也不会为不支持 PDF 或视觉输入的模型生成 OCR 降级结果。[deepagents filesystem.py](/Users/yingshuowang/Documents/back/melonclaw/.venv/lib/python3.12/site-packages/deepagents/middleware/filesystem.py:1811)

**模型能力门控由运行时框架提供，但对当前所有可用模型都不生效。** FilesystemMiddleware 会按 `ModelProfile` 字段过滤多模态块（`deepagents/middleware/filesystem.py:191-197`）：

~~~python
_PROFILE_FIELD_BY_BLOCK_TYPE = {
    "image": "image_inputs",
    "audio": "audio_inputs",
    "video": "video_inputs",
    "file": "pdf_inputs",
}
_TOOL_MESSAGE_FIELD_BY_BLOCK_TYPE = {"image": "image_tool_message", "file": "pdf_tool_message"}
~~~

被判定为不支持的块会被替换成文本占位符而不是抛错（`filesystem.py:237-247`）：

~~~text
[read_file: /x/a.png was not attached because this model does not support image content (image/png).]
~~~

判定规则是“缺失字段默认支持，只有显式 `False` 才拒绝”（`filesystem.py:211-215`）。`profile` 是 `BaseChatModel` 上的 pydantic 字段（`langchain_core/language_models/chat_models.py:366`），`ChatOpenAI._resolve_model_profile()` 只会查 `langchain_openai/data/_profiles.py`（`langchain_openai/chat_models/base.py:1343-1344`），而该表只收录 `gpt-*` 系列，不含 DeepSeek / MiniMax 的模型名，因此 `model.profile` 为 `None`，门控全部失效。

结论：能力声明不能依赖框架自动推导，必须由 MelonClaw 显式注入（见 9.2），否则框架会把 image / file 块原样发给不支持它们的 provider。

**非 PDF 文件块存在一条类级白名单，会误放行。** `filesystem.py:93-102` 把 `(AzureChatOpenAI, ChatOpenAI)` 硬编码为“接受非 PDF file 块”，与 `profile` 无关（`filesystem.py:199-201`）。MelonClaw 的 DeepSeek / MiniMax 正是经 `ChatOpenAI` 接入（`src/melonclaw/core/chat_model.py:23-28`），所以 `.ppt/.pptx` 的 file 块会绕过门控被发送。**`.ppt/.pptx` 不能依赖 read_file 原生文件块路径。**

**上下文压缩只改写发往模型的消息，不清理图状态。** 摘要触发时 `_offload_inline_media` 会把内联 `data:` 媒体上传到 backend 并改写为路径引用块（`deepagents/middleware/summarization.py:1037-1104`），路径形如 `{artifacts_root}/conversation_history/media/{sha256[:16]}.{ext}`；上传失败的块会变成 `<image error="failed_to_offload" />`。但 `wrap_model_call` 只做 `request.override(messages=...)`，源码注释明确写着 "this does NOT modify the LangGraph state"（`summarization.py:1343, 1444-1456`）。

含义是：**一旦 base64 作为消息内容块进入图状态，它会一直留在 PostgreSQL Checkpointer 里；摘要只能让模型看不到，不能让记录消失。**这直接决定 9.1 的实现方式，取舍见 19.2。

官方 Multimodal 文档建议：支持的媒体可以通过标准内容块传入；文件可以放到 backend/filesystem 后由 read_file 按需读取；大媒体应尽量使用引用，避免长期把 base64 放入上下文。多模态内容在文本摘要和上下文压缩时可能被丢弃，因此长期链路必须保留附件引用和可重新读取的文件。[官方 Multimodal 文档](https://docs.langchain.com/oss/python/deepagents/multimodal)

结论：框架提供文件读取、多模态消息承载和“按能力替换为占位符”的降级机制；附件生命周期、权限、暂存、持久化、PDF 文本提取、OCR、能力声明注入、路由决策和沙箱边界必须由 MelonClaw 实现。

### 3.3 LangChain 与 OpenAI-compatible 适配层事实

标准内容块到 provider 请求体的转换由 LangChain 提供，MelonClaw 不需要自己实现这类“翻译器”：

- 转换入口：`_format_message_content`（`langchain_openai/chat_models/base.py:313-345`）；
- 数据块转换：`convert_to_openai_data_block`（`langchain_core/messages/block_translators/openai.py:58-134`）；
- 当前固定走 chat/completions：`_model_prefers_responses_api` 只对 `gpt-5` / `o` 系列 / 模型名含 `codex` 的情况返回 True（`base.py:613-616`），我们的模型名不会触发。

实际输出形状：

| 标准块 | chat/completions 请求体 |
|---|---|
| `{"type": "image", "base64": ..., "mime_type": "image/png"}` | `{"type": "image_url", "image_url": {"url": "data:image/png;base64,..."}}` |
| `{"type": "file", "base64": ..., "mime_type": "application/pdf"}` | `{"type": "file", "file": {"file_data": "data:application/pdf;base64,...", "filename": "..."}}` |

两条必须记住的限制：

1. **chat/completions 路径下文件不能用 URL 传递。** 当 `api == "chat/completions"` 且块中出现 `url` 时，`block_translators/openai.py:126-129` 直接 `raise ValueError("OpenAI Chat Completions does not support file URLs.")`；只有 Responses API 支持 `input_file.file_url`。我们的链路不会走 Responses API，所以 9.1 里“优先使用短时签名 URL 或 provider file ID”在 DeepSeek / MiniMax 上不可行——`file_id` 同样依赖 OpenAI 的文件上传接口。
2. **read_file 产生的 file 块没有 `filename`。** `deepagents/middleware/filesystem.py:1903-1908` 只写入 `type/base64/mime_type`；适配层在缺 filename 时会填占位符 `LC_AUTOGENERATED` 并 `warnings.warn`（`block_translators/openai.py:106-115`）。兼容端点可能依赖文件名判断类型，因此不能“顺手”使用原生 file 块，需要时由 MelonClaw 构造带 `filename` 的块。

将来切换到 Claude / Gemini 时，对应转换分别由 `langchain_anthropic` / `langchain_google_genai` 提供，仍然不是 MelonClaw 的职责，只需替换 model class。

结论：**翻译层是框架职责；“该不该发”的路由判断和“被拒之后怎么办”的兜底是 MelonClaw 职责。**

## 4. 总体方案

采用“规范化业务模型 + 持久化文件 + 运行时沙箱引用”的混合方案：

~~~text
浏览器选择附件
        ↓
上传暂存接口（multipart）
        ↓
AttachmentService 校验、哈希、存储
        ↓
chat_attachments 元数据（staged / ready）
        ↓
发送消息 {content, attachment_ids}
        ↓
服务层重新校验并在消息事务中绑定附件
        ↓
解析/物化到受控 Project 工作区或 Sandbox
        ↓
当前轮图片块、PDF 文件块或派生 Markdown
        ↓
Agent / read_file / OCR / 文档解析
        ↓
大模型
        ↓
消息只持久化文本、附件引用和解析状态
~~~

物理层采用 Yuxi 类似的“临时上传、确认绑定、工作区使用”思路；逻辑层采用独立附件表和消息关系表，不把附件元数据塞在 Conversation JSON 中作为唯一事实来源。

### 4.1 生命周期

附件状态建议为：

~~~text
staged → ready → attached → processing → processed
                         ↘ failed
staged / failed → expired → deleted
任意校验失败 → rejected
~~~

- staged：上传完成但尚未绑定到消息；可以在前端草稿中暂存。
- ready：文件已完成安全校验并可被引用。
- attached：已和某条用户消息建立关系。
- processing：正在提取文本、OCR 或生成派生文件。
- processed：解析结果可用。
- failed：解析失败，但原始文件可能仍可下载或重试解析。
- expired / deleted：不可再被聊天引用，后台清理物理文件。

初版不强制新增独立的“确认接口”：上传接口产生 staged/ready 附件，发送消息时通过 attachment_ids 完成绑定；绑定和 user message 落库必须在同一事务中完成。若未来前端需要显式确认，可以在不改变数据模型的情况下增加 confirm API。

## 5. 支持类型与能力矩阵

上传白名单必须同时校验扩展名、声明 MIME、文件头 magic bytes 和解析结果，不能只信任浏览器的 Content-Type。

| 类型 | 初版策略 | 多模态模型 | 文本模型 | 备注 |
|---|---|---|---|---|
| PNG/JPG/JPEG | 支持 | 当前轮图片内容块或 read_file | OCR 文本 | 上传期统一转 JPEG/PNG |
| WEBP/GIF | 支持 | 同上 | OCR 文本 | 是否转码由 provider 白名单决定，见下方约束 |
| HEIC/HEIF | 转码后支持 | 转成 JPEG 后走图片路径 | OCR 文本 | 框架按扩展名生成 `image/heic` 块，provider 普遍不接受，必须在入库前转码 |
| PDF | 支持 | 由 MelonClaw 构造的 file 块 | 文本提取/OCR | 按页处理，支持混合 PDF；不使用 read_file 产出 |
| PPT/PPTX | 受控解析 | 不使用原生 file 块 | 转文本/图片后处理 | 大文件按页或幻灯片处理，见下方约束 |
| TXT/MD/CSV/JSON | 支持 | 普通文本读取 | 普通文本读取 | 限制编码和大小 |
| DOCX/XLSX | 建议支持 | 不使用原生 file 块 | 解析成 Markdown/结构化文本 | read_file 会走 text 分支解码失败，必须用解析器 |
| 音频/视频 | 后续 | 依赖模型和抽帧/转录能力 | 转录文本 | 不纳入第一期 |
| 可执行文件、脚本、宏、未知二进制 | 拒绝或仅保存元数据 | 不发送给模型 | 不解析 | 不自动执行 |

“上传支持”不等于“当前模型可以理解”。API 返回的附件类型和能力状态必须让前端能够显示“已上传但暂不可解析”。

三条由框架源码推出的强制约束（依据见 3.2、3.3）：

1. **`.ppt/.pptx` 禁止走 read_file 原生 file 块。** 框架把 `ChatOpenAI` 列为“接受非 PDF file 块”，会绕过能力门控直接发送；并且 Deep Agents 生成的 file 块不含 `filename`。
2. **`.docx/.xlsx` 禁止走 read_file。** 不在扩展名映射表内，会按 `text` 类型以 `utf-8` 严格模式打开二进制内容而解码失败。
3. **`.heic/.heif` 必须在入库前转码。** 能力门控只按块类型（`image_inputs`）判断，不校验 MIME，因此 HEIC 会被放行给不接受该 MIME 的 provider。

上传白名单必须与 3.2 的扩展名映射表对齐：映射表之外的格式只能交给 MelonClaw 自己的解析器，不能依赖 read_file。

## 6. API 设计

### 6.1 上传附件

~~~http
POST /api/projects/{project_id}/attachments
Content-Type: multipart/form-data
~~~

表单字段：

- file：上传文件；
- user_id、tenant_id：仅保留开发模拟入口兼容，服务层仍需重新校验；生产认证接入后以服务端身份为准；
- 可选 client_request_id：用于上传重试幂等。

响应：

~~~json
{
  "attachment_id": "att_01...",
  "file_name": "report.pdf",
  "media_type": "application/pdf",
  "kind": "pdf",
  "size_bytes": 183421,
  "sha256": "...",
  "status": "ready",
  "preview": {
    "available": false,
    "parse_status": "pending"
  }
}
~~~

响应中不返回宿主机绝对路径、对象存储密钥、原始 base64 或可绕过鉴权的永久 URL。

### 6.2 发送聊天消息

扩展现有 JSON：

~~~json
{
  "user_id": "dev-user",
  "tenant_id": "dev-tenant",
  "request_id": "req_01...",
  "content": "请总结这个报告",
  "model_id": "system:model",
  "skill_id": null,
  "attachment_ids": ["att_01..."]
}
~~~

服务层必须通过 conversation_id 查询真实 Conversation 和 Project，再校验：

1. 当前用户是否属于请求租户；
2. Conversation 是否属于当前用户和 Project；
3. 每个附件是否属于当前用户和 Project；
4. 附件状态是否为 ready、attached 或允许重用的 processed；
5. 附件类型、数量、单条消息总大小是否符合限制；
6. 同一 request_id 重试时，正文、模型、技能和附件集合是否完全一致。

### 6.3 查询与下载

建议增加：

~~~http
GET    /api/attachments/{attachment_id}
GET    /api/attachments/{attachment_id}/content
DELETE /api/attachments/{attachment_id}
~~~

所有接口都执行服务端归属校验。内容接口通过鉴权 API 或短时签名 URL 返回，不能把上传目录挂成静态资源。跨用户、Project 或 Conversation 的资源访问统一返回不存在或无权访问，避免附件 ID 枚举。

### 6.4 SSE 与历史消息

SSE 只传附件摘要，不传二进制：

~~~json
{
  "attachment_id": "att_01...",
  "file_name": "report.pdf",
  "media_type": "application/pdf",
  "size_bytes": 183421,
  "parse_status": "processed"
}
~~~

message_started 可以携带当前消息附件摘要；completed、failed、approval_required 和 HITL resume 继续使用现有消息状态语义。断线重连和历史读取通过附件 ID 重新查询，不依赖 SSE 中缓存文件内容。

## 7. 数据模型

### 7.1 chat_attachments

建议字段：

| 字段 | 说明 |
|---|---|
| id | 附件 ID，建议 UUID/不可猜测 ID |
| user_id | 业务所有者 |
| project_id | 物理工作区归属 |
| original_name | 展示用原文件名，不能直接作为路径 |
| media_type | 服务端探测后的 MIME |
| kind | image/pdf/document/text/other |
| size_bytes | 实际字节数 |
| sha256 | 完整性校验和，可用于去重 |
| storage_key | 受控存储键，不直接暴露 |
| sandbox_path | 运行时虚拟路径，可按需生成 |
| derived_storage_key | OCR/解析结果路径，可为空 |
| status | 生命周期状态 |
| parse_status | pending/processing/processed/failed/not_required |
| parse_error_code | 脱敏错误码 |
| request_id | 可选的上传/绑定幂等关联 |
| created_at / updated_at | 生命周期时间 |
| expires_at | 暂存附件过期时间 |

不在表中保存长期 base64。若需要保存 OCR 摘要，保存有限的统计信息和派生文件引用，不把完整解析正文当作附件元数据 JSON。

### 7.2 chat_message_attachments

~~~text
message_id       FK chat_messages.id
attachment_id    FK chat_attachments.id
ordinal          当前消息内的显示/处理顺序
created_at
~~~

主键为 message_id、attachment_id；对 message_id、ordinal 建唯一约束。关系写入和用户消息、助手 pending 消息必须与现有消息对创建在同一事务中。

附件的长期物理归属是 user_id + project_id，消息关系决定它在哪些 Conversation/Message 中被引用。由于同一 Project 下的 Conversation 共享工作区，运行时不能把整个 Project 附件目录无差别挂给 Agent，必须只挂载当前运行授权的附件集合。

## 8. 物理存储与路径

### 8.1 本地开发

当前配置使用 MELONCLAW_WORKSPACE_DIR；若未配置，默认工作区根目录是 ~/.melonclaw/workspaces。建议附件目录如下：

~~~text
~/.melonclaw/workspaces/projects/project_123/
└── attachments/
    └── attachment_456/
        ├── original/
        │   └── report.pdf
        └── derived/
            ├── report.parsed.md
            └── pages/
                ├── page-001.png
                └── page-002.png
~~~

### 8.2 生产环境

推荐对象存储保存原始文件，沙箱只在运行时挂载或复制必要文件：

~~~text
对象存储：
s3://melonclaw-attachments/project_123/attachment_456/original/report.pdf

沙箱虚拟路径：
/attachments/attachment_456/original/report.pdf
/attachments/attachment_456/derived/report.parsed.md
~~~

对象存储或持久化文件系统是原始附件的事实来源；沙箱是运行时执行边界，不应成为唯一的业务元数据来源。原始附件和派生解析文件只读挂载。Agent 编辑或生成的文件写入：

~~~text
/project/...
/outputs/...
~~~

不要使用用户原始文件名拼接真实路径；路径由附件 ID 和固定目录生成，并执行 resolve、root containment、no-follow/symlink 检查。

建议提炼：

~~~text
AttachmentStorage
├── LocalAttachmentStorage       # 本地开发
└── ObjectAttachmentStorage      # 生产对象存储

AttachmentRuntime
├── materialize_for_local_backend
└── mount_for_sandbox
~~~

这样未来从当前 LocalShellBackend 切换到安全 SandboxBackend 时，API、数据库和 Agent 的逻辑路径保持稳定。

**工作区布局必须显式约束框架自身的产物。** 当前 `build_agent_backend` 构造 `CompositeBackend(default=runtime_backend, routes=routes)` 时没有传入 `artifacts_root`（`src/melonclaw/backend/factory.py:138-141`），框架默认值是 `"/"`（`deepagents/backends/composite.py:210`）。这意味着摘要中间件卸载的会话历史和媒体会直接落进 Project 工作区根目录：

~~~text
<project workdir>/
├── conversation_history/
│   ├── {thread_id}.md                    # 卸载的历史记录
│   └── media/{sha256[:16]}.{ext}         # 从消息中卸载的多媒体
├── attachments/                          # 本方案的附件目录
└── ...                                   # 用户与 Agent 的文件
~~~

建议显式设置 `artifacts_root`，把框架产物与 `attachments/`、用户文件分开：否则 Agent 的 ls/glob 会把框架内部文件当成用户数据，配额统计和清理也会互相干扰。

## 9. 图片处理与模型能力降级

### 9.1 支持视觉模型

当前消息发送时，服务层根据附件 ID 获取已授权图片，并在 Agent 调用边界做 hydration：

~~~text
消息文本 + 当前轮图片附件引用
             ↓
读取沙箱中的图片
             ↓
生成标准 image content block
             ↓
由 LangChain 适配层转换成本次 provider 需要的请求格式（见 3.3）
             ↓
发送给大模型
~~~

**base64 的落点必须显式设计，不能依赖框架自动清理。** 目标不变：base64 不出现在 `chat_messages.content`、SSE、Store 和长期消息历史中。但 3.2 已确认两件事：

- base64 一旦作为消息内容块进入 LangGraph 图状态，就会随 Checkpointer 落库；摘要中间件只改写发往模型的消息，不删除状态里的块；
- 摘要触发时框架还会把内联媒体上传到 `{artifacts_root}/conversation_history/media/`，本地模式下会写进 Project 工作区。

所以当前轮图片只有两种可选实现方式，取舍见 19.2：

- 方案 A（接受落库）：直接把 image block 放进 HumanMessage。实现最简单，但必须配套单文件与单消息大小上限、Checkpointer 清理策略和必要的存储加密。
- 方案 B（请求期注入）：新增自定义 middleware，在 `wrap_model_call` 中按附件 ID 从 AttachmentStore 读取图片并 `request.override(messages=...)`，图状态只保留文本和附件 ID。这与框架自己处理摘要的做法同构；若选此方案，必须同时阻断 read_file 的媒体结果，否则媒体仍会以框架合成的消息进入状态。

明确不可用的做法：在 chat/completions 路径下用短时签名 URL 或 provider file ID 传文件（原因见 3.3）。

当前轮图片可以直接 hydration，让模型立即看图；历史轮只保存附件 ID、文件名、类型和虚拟路径，后续需要时重新读取，避免每轮重复发送大图片。

### 9.2 文本模型或不支持当前媒体类型

如果模型能力声明不包含 image 或 file：

1. 不发送原生图片/PDF 内容块；
2. 对图片调用 OCR Provider；
3. 对扫描 PDF 调用页面级 OCR；
4. 将 OCR 结果保存成派生 Markdown；
5. 把派生文件路径和相关文本片段交给文本模型；
6. 如果用户要求识别图表趋势、页面布局或非文字视觉内容，而系统只有 OCR 模型，应明确告知能力边界，不能把 OCR 结果伪装成完整图片理解。

当前模型目录需要增加能力声明，但不再自造字段名，而是映射到运行时框架已有的 `ModelProfile`（3.2）：

~~~python
# model_catalog 侧声明（平台配置）
input_modalities: frozenset[str]      # text / image / file
supports_pdf: bool
supports_tool_media: bool

# 注入 ChatModel 侧：门控读取的就是这张表
profile = {
    "image_inputs": "image" in input_modalities,
    "pdf_inputs": supports_pdf,
    "image_tool_message": supports_tool_media,
    "pdf_tool_message": supports_tool_media,
}
model = model.model_copy(update={"profile": profile})
~~~

注入位置建议在 `build_chat_model`（`src/melonclaw/core/chat_model.py`）或 Agent 组装处。注入后框架会自动把不支持的块替换成文本占位符，因此“不伪装成理解”不需要额外实现。

注意 `profile` 只能按块类型生效，无法表达 MIME 级差异（例如“支持 image 但不支持 HEIC”），MIME 级判断仍由 5 的上传白名单和本节的能力路由负责。

能力声明是平台配置，不允许由浏览器任意提交。真正发送前仍需要 provider 兼容性测试和错误降级（见 14）。

能力路由是 MelonClaw 的职责：给定（provider、模型能力、附件类型）决定走 `native_image` / `native_file` / `ocr_text` / `unsupported` 哪条路径，并记录本次选择（见 15）。框架只负责执行，不负责决策。

## 10. PDF 处理设计

### 10.1 页面级判断

不能只按整份 PDF 判断“文字版”或“扫描版”，因为 PDF 可能混合文字页、扫描页、截图页和图表页。处理器逐页执行：

~~~text
读取 PDF
  ↓
逐页提取文本层
  ↓
文字量达到阈值 → 保留提取文字
文字为空/过少 → 页面渲染成图片
  ↓
调用 OCR Provider
  ↓
按页码合并 Markdown
~~~

页级结果至少保留：页码、文本、来源类型（embedded_text 或 ocr）、可选置信度和错误状态。这样用户看到答案时可以定位到原 PDF 页码。

### 10.2 支持 PDF 的多模态模型

如果当前模型明确支持 PDF，Agent 可以调用 read_file：

~~~text
/attachments/attachment_456/original/report.pdf
        ↓
read_file
        ↓
标准 file content block
        ↓
provider adapter
        ↓
模型理解 PDF
~~~

这条路径由 Deep Agents 提供基础能力，但 PDF 的实际解析和图片理解仍由模型供应商负责。不能假定所有 OpenAI-compatible endpoint 都接受 PDF file block。

### 10.3 不支持 PDF 的文本模型

由 MelonClaw 的 PdfParser 生成派生文件：

~~~text
/attachments/attachment_456/derived/report.parsed.md
~~~

处理顺序：

1. 使用 PDF 文本提取器读取可复制文本；
2. 对无文本页渲染图片；
3. 调用用户提供的 OCR 模型；
4. 对表格尽可能保留 Markdown 表格或结构化块；
5. 对提取失败页记录页码和脱敏错误；
6. Agent 使用 read_file 按需读取派生 Markdown，而不是一次性把全文注入上下文。

### 10.4 大 PDF

大 PDF 不一次性发送给模型：

- 生成页码索引和目录摘要；
- 按页或按段落读取；
- 必要时增加 search_attachment 或解析结果检索工具；
- ToolMessage 超过上下文阈值时返回派生文件路径，让 Agent 继续读取；
- 将解析缓存和原文件哈希关联，文件未变化时不重复 OCR。

## 11. OCR Provider 接入

用户将提供一个 OCR 模型。MelonClaw 不应把它硬编码进 PDF 处理器，而应定义稳定的 Provider 接口：

~~~python
class OCRProvider(Protocol):
    async def recognize_image(
        self,
        image_bytes: bytes,
        *,
        mime_type: str,
        language: str | None = None,
        request_id: str | None = None,
    ) -> OCRResult: ...
~~~

建议标准化返回：

~~~python
class OCRResult(TypedDict):
    text: str
    blocks: list[OCRBlock]
    confidence: float | None
    language: str | None
    provider: str
    model: str
~~~

如果模型只返回纯文本，blocks 可以为空；如果支持文字框、表格或页内坐标，应保留这些结构，便于后续生成更好的 Markdown。

OCR 配置建议通过环境变量提供，具体名称在实现时以仓库现有配置风格为准，例如：

~~~text
MELONCLAW_OCR_PROVIDER
MELONCLAW_OCR_BASE_URL
MELONCLAW_OCR_MODEL
MELONCLAW_OCR_API_KEY
~~~

不要把真实 Key 写入本文档、代码、测试、日志或回复。接入前需要确认：

- 模型接受图片还是也接受 PDF；
- 返回纯文本还是结构化结果；
- 中文、英文、表格、旋转文字和低清扫描件效果；
- 单页大小、超时、并发和速率限制；
- 是否可以在本地/沙箱内访问；
- 失败是否可重试，是否有重复计费风险。

OCR 只解决文字识别。如果要回答“这张图表的趋势是什么”或“截图中的按钮是什么意思”，需要另外配置视觉理解模型；不能把 OCR Provider 当作通用 Vision Provider。

## 12. Agent 集成边界

建议新增职责清晰的模块，而不是把逻辑全部塞进 services/chat.py：

~~~text
src/melonclaw/
├── api/routes/attachments.py
├── api/schemas.py                         # Attachment DTO、MessageRequest 扩展
├── repository/attachments.py
├── storage/attachments.py                 # 本地/对象存储
├── services/attachments.py                # 生命周期、授权入口、绑定
├── services/attachment_parser.py          # 图片/PDF/办公文件解析
├── services/ocr.py                        # OCRProvider 与配置
├── services/attachment_runtime.py         # 物化、沙箱挂载、虚拟路径
├── database/schema.py
└── database/migrations.py
~~~

Agent 侧建议增加一个附件上下文构造器：

~~~text
build_attachment_context(
    authorized_attachments,
    resolved_model_capabilities,
    runtime_backend,
)
~~~

它只接收服务层已经校验过的附件，输出：

- 当前轮图片且模型支持视觉：`native_image`，生成标准 image content block；
- PDF 且模型声明 `pdf_inputs`：`native_file`，由 MelonClaw 构造带 `filename` 的 file 块（不使用 read_file 的产出，见 3.3）；
- 模型不支持当前媒体类型：`ocr_text`，给出派生 Markdown 的虚拟路径和少量摘要；
- 没有降级能力的类型：`unsupported`，返回安全的错误状态，不输出内部存储细节。

它输出的能力判断必须和 9.2 注入 ChatModel 的 `ModelProfile` 来自同一份能力声明，避免出现“路由认为不支持、门控认为支持”的双重事实。

不要让 Agent 自己根据用户提交的字符串访问任意本地路径。工具只能看到虚拟路径，后端负责把虚拟路径映射到授权文件系统。

## 13. 安全边界

### 上传校验

- 扩展名、MIME、magic bytes 三者交叉验证；
- 流式读取并限制单文件、单消息和单 Project 配额；
- 文件名只作展示，去除路径分隔符、控制字符和 ..；
- 图片限制像素总数，防止解码内存攻击；
- PDF/压缩包限制页数、递归层数、解压大小和解析时间；
- 计算 SHA-256；
- 可选病毒扫描，扫描失败默认不可进入 Agent；
- 不自动解压、执行脚本、打开宏或调用外部程序。

建议初始配置：单文件 20 MB、单条消息最多 10 个附件、单条消息总计 50 MB。实际配置应使用环境变量并写入 README，不能散落在路由代码中。

### 运行与解析

- 原始文件和派生文件只读挂载；
- 沙箱关闭不必要的网络、进程、设备和宿主目录访问；
- 解析器与主 Web 进程隔离，尤其是 PDF、Office 和图片解码器；
- 不把附件内容直接当作系统指令；Prompt 中明确附件内容是不可信数据；
- 虚拟路径必须做 root containment、symlink/no-follow 和 Project 边界检查；
- 解析产生的派生文件使用随机 ID 目录，不能由用户文件名决定；
- 下载和预览必须重新校验用户、租户、Project、Conversation 和附件关系；
- 框架自身的卸载产物（`conversation_history/`、`conversation_history/media/`）必须落在受控的 `artifacts_root`，不能和用户文件、附件目录混放（见 8）；
- 不做容器格式的原生解析：`.ppt/.pptx/.docx/.xlsx` 一律交给 MelonClaw 的解析器，不把用户文件直接交给 read_file 或 provider 解析（依据 3.2）。

当前 LocalShellBackend 只能作为本机开发实现。未来安全沙箱接入前，不能把该能力部署给不受信任的共享用户。

## 14. 失败、重试和幂等

建议状态码：

| 状态码 | 场景 |
|---|---|
| 400 | multipart 参数或请求格式错误 |
| 404 | 附件不存在、已删除或不属于当前资源 |
| 409 | request_id 重试时附件集合与首次请求不一致 |
| 413 | 文件、消息或 Project 配额超限 |
| 415 | MIME、扩展名或类型不支持 |
| 422 | 解析失败、模型不支持且没有降级能力 |
| 503 | 对象存储、OCR、解析 worker 或 provider 暂不可用 |

规则：

- 上传重试可以使用 client_request_id 或内容哈希去重；
- 消息重试必须复用相同 request_id，正文、模型、技能和附件 ID 集合不能变化；
- OCR/解析失败不删除原始附件，允许重试；
- 消息执行失败不自动删除已绑定附件；
- 暂存附件超过 TTL 自动清理，例如 24 小时；
- 删除附件前检查是否仍被排队请求、活动运行或历史消息引用；
- 物理删除失败时保留数据库状态和审计信息，不把删除失败伪装成成功。

## 15. 可观察性

日志和指标必须脱敏，不记录文件内容、base64、API Key 和宿主机绝对路径。

建议记录：

- attachment_id、project_id、request_id 的脱敏或完整内部 ID；
- 文件类型、大小、哈希前缀、状态变化；
- 上传、绑定、解析、OCR、挂载的耗时；
- OCR provider、模型名、页数、成功/失败页数；
- 模型输入能力选择：native_image、native_file、ocr_text、unsupported；
- 失败错误码和重试次数；
- staged 清理数量、解析队列长度和存储失败数量。

禁止记录：

- 原始文件内容；
- 图片 base64、PDF base64；
- 用户上传文件的敏感文本；
- API Key、Token、数据库密码；
- 可暴露宿主机布局的完整绝对路径。

## 16. 实施顺序

### Phase 0：技术验证

Phase 0 的产出是实测记录，不是设计推断。以下事项必须先有结论，否则后续 Phase 的接口、路由表和表结构都要返工：

1. **能力声明与门控**：打印实际模型的 `model.profile`，确认当前为 `None`；再注入一版 `profile`，确认不支持的块被替换成文本占位符（依据 3.2）。
2. **provider 真实接受度**：分别用 DeepSeek、MiniMax 实测 image 块（`image_url` data URL）、PDF file 块、`.pptx` file 块，记录真实 HTTP 状态码和错误体。这一项直接决定 9.2 能力路由表的内容。
3. **read_file 行为**：验证图片、PDF、`.pptx`、`.docx` 四类文件的 read_file 结果，确认 3.2 的结论（尤其 `.docx` 解码失败、file 块缺少 `filename`）。
4. **图状态与落库**：确认 base64 是否随 Checkpointer 落库、摘要触发后是否仍留在状态里，据此在 19.2 中拍板方案 A 或 B。
5. **事件流影响**：确认 read_file 的媒体结果（框架以带 `read_file_media_result` 标记的合成消息承载）是否会进入现有 SSE 事件适配器，避免前端把它当成一条用户消息渲染。
6. **框架产物路径**：确认 `artifacts_root` 的实际落点，以及是否需要显式配置（见 8）。
7. **OCR Provider**：确认用户提供的 OCR 模型接口、输入形式（图片还是 PDF）、返回结构和错误语义（见 11）。
8. **PDF 逐页判断**：用文字 PDF、扫描 PDF、混合 PDF 各一份验证页级分类阈值。
9. **资源边界**：确认本地开发模式下的文件大小、解析超时和解析进程边界。

### Phase 1：基础存储与数据库

1. 增加 chat_attachments、chat_message_attachments；
2. 增加迁移、schema 版本和 Repository；
3. 实现 AttachmentStorage 本地后端；
4. 实现上传校验、哈希和 staged/ready 状态；
5. 增加元数据、下载和删除接口。

### Phase 2：聊天绑定与历史

1. 为 MessageRequest 增加 attachment_ids；
2. 在 ExecutionService.prepare_message() 中完成归属、状态、配额和幂等校验；
3. 在同一事务中写消息附件关系；
4. 扩展历史消息和 SSE 的附件摘要；
5. 验证无附件旧客户端兼容、request_id 冲突和 HITL resume。

### Phase 3：解析与 Agent 输入

1. 增加模型能力声明（映射为 `ModelProfile` 并注入 ChatModel）和输入路径路由（native_image / native_file / ocr_text / unsupported）；
2. 实现图片原生 hydration（落库方式按 19.2 的最终决策执行）；
3. 实现 PDF 文本层提取；
4. 接入 OCRProvider，支持扫描页和混合 PDF；
5. 生成派生 Markdown，并让 Agent 按需读取；
6. 对大文件增加分页、分块或搜索能力；
7. 对不支持模型实现明确错误和降级行为。

### Phase 4：前端

1. Composer 增加文件选择、上传进度、失败重试和删除；
2. 支持图片缩略图和普通文件卡片；
3. 发送前等待附件进入 ready 状态；
4. 消息气泡显示附件摘要和解析状态；
5. 历史消息通过鉴权接口预览/下载；
6. 切换 Project、Conversation 或用户时取消未完成上传并丢弃迟到响应。

### Phase 5：沙箱与生产化

1. 将本地工作区存储抽象替换为对象存储或持久 UserWorkspace；
2. 将附件按当前运行授权集合只读挂载到 SandboxBackend；
3. OCR/PDF/Office 解析迁移到隔离 worker；
4. 关闭沙箱网络、宿主目录和不必要的 Shell 能力；
5. 完成多租户、配额、病毒扫描、清理和审计验证。

## 17. 验收标准

### 功能

- 可以上传合法图片、文本 PDF、扫描 PDF 和混合 PDF；
- 图片模型能看当前轮图片；
- 文本模型能读取图片 OCR 结果；
- `.ppt/.pptx/.docx/.xlsx` 由 MelonClaw 解析器生成派生文本，不依赖 read_file；
- `.heic/.heif` 在上传期转码后可作为图片使用；
- 支持 PDF 的模型可以使用由 MelonClaw 构造的 PDF file 块；
- 不支持 PDF 的模型可以读取 parsed.md；
- 混合 PDF 文字页和扫描页都能保留页码；
- 历史消息显示附件摘要，并能通过授权接口下载；
- Agent 生成文件进入 /project 或 /outputs，不修改原始附件。

### 安全

- 跨用户、跨租户、跨 Project、跨 Conversation 的附件无法读取；
- 路径穿越、软链接逃逸、伪造 MIME、超大文件和恶意文件名被拒绝；
- 原始文件不进入日志、SSE 和消息正文；base64 在 Checkpointer 中的存留范围与 19.2 的最终决策一致，并在文档中如实标注；
- 无 OCR/视觉能力时不会伪装成已经理解图片；
- 当前 LocalShellBackend 的非沙箱限制在 README 和运行提示中明确展示。

### 一致性

- request_id 重试得到相同的附件集合和消息结果；
- 断线重连、历史读取和 HITL resume 可以重新定位附件；
- staged 附件能过期清理；
- 解析失败可重试，不破坏原始文件；
- 数据库迁移可重复执行，旧消息和无附件请求保持兼容。

## 18. 验证计划

本地静态验证：

~~~bash
uv lock --check
uv run python -m compileall -q src
uv run pytest -q
uv run ruff check src tests
git diff --check
~~~

前端验证：

~~~bash
npm run lint
npm run typecheck
npm test
npm run build
~~~

需要外部服务的验证单独记录：

- PostgreSQL：迁移、事务、并发绑定和幂等；
- 对象存储：上传、下载、过期清理和失败重试；
- OCR 模型：中文/英文、低清、表格、旋转文字、超时和限流；
- 多模态模型：图片/PDF content block、流式和错误降级；
- SandboxBackend：只读挂载、路径隔离、网络限制和资源配额。

## 19. 决策记录

### 19.1 已确认决策

#### 2026-09-11：采用混合方案

物理运行路线采用 Yuxi 的暂存文件、Workdir/Sandbox、Agent 虚拟路径和按需读取；业务数据采用独立附件表和消息关系表，不采用 Conversation JSON 作为唯一附件存储。

#### 2026-09-11：不长期保存 base64

base64 只允许出现在一次 outbound provider request 或框架内部临时消息中。数据库、历史消息、SSE、Store 和 Checkpointer 只保存附件 ID、元数据、虚拟路径和派生结果引用。

> 2026-09-12 复核：该结论在当前框架用法下不成立。Deep Agents 会把消息中的 base64 块写入 LangGraph 状态，Checkpointer 不会自动清理；摘要中间件只改写发往模型的消息。需在 19.2 重新拍板实现方式。

#### 2026-09-11：图片和文件分两条模型路径

支持多模态的当前轮图片可以原生 hydration；普通文件优先走 Agent filesystem / read_file。文本模型使用 OCR 或文档解析生成 Markdown。OCR 不被定义为通用视觉理解。

> 2026-09-12 复核：「普通文件走 read_file」的范围需要收窄。`.ppt/.pptx/.docx/.xlsx` 必须由 MelonClaw 解析器处理，依据见 3.2 与 5。

#### 2026-09-12：确认 Deep Agents 的 PDF 边界

Deep Agents read_file 可以将 PDF 转成 file content block，但不提供通用 OCR 和文本模型降级。PDF 解析必须由 MelonClaw 的页面级 PdfParser 和用户提供的 OCRProvider 补齐；扫描页、文字页和混合 PDF 需要逐页分类。

#### 2026-09-12：能力声明复用运行时框架的 ModelProfile

背景：原计划在 model_catalog 自建 `input_modalities` / `supports_pdf` 能力字段，并自行实现降级判断。

变更：能力声明映射为运行时框架已有的 `ModelProfile`，在 ChatModel 侧注入；不支持块的替换由框架完成，MelonClaw 只负责声明与路由。

原因：Deep Agents 已内建按 `ModelProfile` 过滤多模态块的机制，自建一套属于重复实现；而 DeepSeek / MiniMax 经 `ChatOpenAI` 接入时 `model.profile` 为 `None`，门控默认全部放行，必须显式注入才生效。

影响的章节/API/数据：3.2、3.3、9.2；`core/model_catalog.py`、`core/chat_model.py`。

需要补充的验证：16 Phase 0 第 1、2 项。

#### 2026-09-12：适配层由 LangChain 提供，MelonClaw 只做路由与兜底

背景：需要确认标准内容块到 provider 请求体的转换是否必须自己开发。

变更：明确分工。转换由 `langchain_openai` 与 `langchain_core` 的 block translator 提供；MelonClaw 负责「该不该发」的能力路由和「被拒之后怎么办」的错误降级。

原因：`_format_message_content` 已覆盖 image / file / audio 等标准块到 OpenAI 方言的转换（含 data URL 编码），但框架不判断兼容端点是否接受这种方言；并且框架自身的非 PDF file 块白名单还会误放行。

影响的章节/API/数据：3.3、9.2、14。

需要补充的验证：16 Phase 0 第 2、3 项。

#### 2026-09-12：chat/completions 路径不支持文件 URL 和 provider file ID

背景：原设计在 9.1 中保留了「优先使用短时签名 URL 或 provider file ID」的选项。

变更：删除该选项。文件只能以内联 base64 的 file 块传递。

原因：`convert_to_openai_data_block` 在 chat/completions 下遇到带 `url` 的 file 块会直接抛 `ValueError`；`input_file.file_url` 只在 Responses API 可用，而我们的模型名不会触发 Responses API；`file_id` 依赖 OpenAI 的文件上传接口。

影响的章节/API/数据：3.3、9.1。

需要补充的验证：无（源码可直接判定的限制）。

#### 2026-09-12：扩展名白名单与 Office / HEIC 的强制约束

背景：需要确定哪些格式可以交给 read_file，哪些必须自己解析。

变更：

- `.ppt/.pptx` 不使用原生 file 块；
- `.docx/.xlsx` 不使用 read_file；
- `.heic/.heif` 在上传期转码为 JPEG。

原因：三条都是 0.7.5 源码中可直接判定的行为——框架把 `ChatOpenAI` 硬编码为非 PDF file 块的容忍类型会绕过能力门控且生成的块没有 `filename`；`.docx/.xlsx` 不在扩展名映射表内会按 `text` 以 utf-8 严格解码失败；能力门控只按块类型判断、不校验 MIME。

影响的章节/API/数据：3.2、5、9.2；上传校验白名单。

需要补充的验证：16 Phase 0 第 3 项。

#### 2026-09-12：显式约束框架产物的落点

背景：`build_agent_backend` 构造 `CompositeBackend` 时未设置 `artifacts_root`。

变更：建议显式设置 `artifacts_root`，把摘要卸载产生的 `conversation_history/` 与 `conversation_history/media/` 从 Project 工作区根目录移出，与 `attachments/` 分开。

原因：默认值 `"/"` 会让框架产物落进用户工作区，与用户文件、附件目录混在一起，影响 ls/glob 语义、配额统计和清理。

影响的章节/API/数据：8、13；`backend/factory.py`。

需要补充的验证：16 Phase 0 第 6 项。

### 19.2 待拍板问题

确认后并入 19.1，并同步修改对应章节。

**问题 1：base64 是否允许进入 Checkpointer？**

- 方案 A（接受落库）：当前轮图片直接作为 image block 进入 HumanMessage。实现最简单，但必须配套单文件与单消息大小上限、Checkpointer 清理策略和必要的存储加密。
- 方案 B（请求期注入）：新增 middleware，在 `wrap_model_call` 中按附件 ID 读取图片并 `request.override(messages=...)`，图状态只保留文本与附件 ID；需同时阻断 read_file 的媒体结果。

待定原因：需要 Phase 0 第 4 项实测落库规模，并确认业务上是否把「原始二进制不落库」当作硬约束。

**问题 2：附件目录如何暴露给 Agent？**

`CompositeBackend.routes` 按固定路径前缀注册，运行期无法按请求增删。可选方案：接受同一 Project 下的附件对 Agent 可见（配合 `FilesystemPermission` 只读），或改为按运行授权集合重建 Agent——后者会影响 `services/runtime.py` 中按 `(project_id, model)` 缓存 Agent 的粒度。

待定原因：不拍板则数据模型和 Agent 缓存策略都可能返工。

**问题 3：MIME 级白名单由谁定义？**

能力门控只按块类型，无法表达「支持 image 但不支持 WEBP/HEIC」。需要明确是按 provider 维护 MIME 白名单，还是统一在上传期转码为 JPEG/PNG。

待定原因：依赖 Phase 0 第 2 项的 provider 实测结果。

### 后续记录格式

后续每次设计变更追加：

~~~text
### YYYY-MM-DD：变更标题

背景：
变更：
原因：
影响的章节/API/数据：
需要补充的验证：
~~~

